
TODO
* página, lista todas categorias
* ter opção para add
* dá opção para editar nome, parent, etc

TODO
* listar categorias na janela add rss
* lá, tem que ter opção para adicionar nova, escrevendo o nome do pai

TODO
* add rss
* api: le o valor quando cadastrado (ou iniciado o server) e guarda na memoria
* user subscribe rss da memoria
* api volta a ler o feed, se tiver update, disposable o antigo e add na memoria
* show created rss e checa se atualiza a cada X minutos

TODO
* btn preview content ao add rss

TODO
* ao criar user, criar userGrid default pra ele
* page to user criar userColumn
* show only rss signed by user

TODO
* usuario editar grid
* user set novos userColumns para o grid
* user edit config userColumn

columnsview will always show the most especific signed, them the more generic minus the especifics
- na real acho que nao da nada o mesmo feed aparecer duas vezes, problema do usuário, ele que tire

reddit pode ter a opção de formato (orderby) levando em conta a pontuação

tbl relevance{
userId
target: { feed, topic, word}
level: 0/5 [not set = 3]
}

update .ico from domain every month

API must read the rss each X minutes, and store the value in memory... it will reduce the quantity of calls based on user
- if feed is signed by any user, check updates
- X = avarage distance period of news in a same week day; ***
- otherwise, 15 min default
{
g1 html = 322.78 KB per read
omelete = 2.32 MB
gizmodo feed = 57.17 KB
}
